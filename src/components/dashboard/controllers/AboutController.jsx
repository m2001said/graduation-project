import React from 'react'
const AboutController = () => {
  return (
    <div>AboutController</div>
  )
}
export default AboutController


// import React from "react";
// import Shared from "./Shared";
// import { v4 as uuidv4 } from "uuid";

// const SubController = ({ blocks, dispatchRef }) => {
//   return (blocks.map((block, index) => {
//     console.log(block.cardCaption.concat(block.cardTitle))
//     return <Shared dispatchFun={dispatchRef} index={index} block={block} key={block.cardCaption.concat(block.cardTitle)} />;
//   }))
// };

// export default SubController;